# repositorio
repositorio

## Vincular o ssh na sessão
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

 git config --global user.email "you@example.com"
 git config --global user.name "Your Name"

## Nova Feature
Nova Branch
    Branch Develop
        Branch Main


## Git Flow
main        
hotfix
release
develop
feature
feature

git checkout -b xxx
git checkout xxx
git merge xxx


## GitHub Flow
change
main
change
change

## Pipeline
teste de pipeline

